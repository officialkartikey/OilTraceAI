"use client";
import React from 'react';

// SimulationTimeline has been removed as per specification.
// The map now expands naturally into the available vertical space.
export default function SimulationTimeline() {
  return null;
}
